<%= render :partial => "header" %>

<%= render :partial => 'form', :locals => { :title => t("contacts.new.title"), :remote => false } %>

<%= link_to t('helpers.forms.back'), customer_contacts_path(@customer) %>
