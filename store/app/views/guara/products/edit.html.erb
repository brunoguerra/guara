<%= render :partial => "navbar", :locals => { :active => :edit } %>

<%= render :partial => 'form', :locals => { :title => t("products.edit.title") } %>

<%= link_to t('helpers.forms.back'), product_path(@product.id), :class => "btn" %>
