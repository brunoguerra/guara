<section id="stock">

	<div id="dashboard">
			<navbar>
				<%= render :partial => "navbar", :locals => { active: :none } %>
			</navbar>

			<%= render :partial => 'form', :locals => { :title => t("orders.in.new.title") } %>